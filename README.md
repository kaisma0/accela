# ACCELA

A SLSsteam maanger

## Install

Install everything:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/install-all.sh | bash
```

Install ACCELA only:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/install-accela.sh | bash
```

Install SLS only:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/install-sls.sh | bash
```

## Uninstall

Uninstall everything:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/uninstall-all.sh | bash
```

Uninstall ACCELA only:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/uninstall-accela.sh | bash
```

Uninstall SLS only:

```bash
curl -fsSL https://raw.githubusercontent.com/kaisma0/accela/main/scripts/uninstall-sls.sh | bash
```
